import React, {FC} from 'react';
import {AuthHeader} from '../../../dashboard-layout/components/header/auth-header';

const UserDropdown: FC = () => {
    return <AuthHeader />;
};

export {UserDropdown};
